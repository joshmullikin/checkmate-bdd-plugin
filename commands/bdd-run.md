---
description: Execute BDD scenarios via checkmate with auto-classify-heal-retry on failure. Usage: /bdd-run <scenario-name> | <feature-group> | all
argument-hint: <scenario-name> | <feature-group> | all
---

Run the `bdd:run` skill with the target provided: `$ARGUMENTS`.

If no argument is given, default to `all`.
