---
description: Start, stop, or check the BDD service stack (checkmate + playwright-http + checkmate-mcp + app under test). Usage: /bdd-stack up | down | status
argument-hint: up | down | status
---

Run the `bdd:stack` skill with the argument provided: `$ARGUMENTS`.

If no argument is given, default to `status`.
